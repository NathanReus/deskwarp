# api-models

Serializable data types used by the DeskWarp HTTP API.  
Both the daemon and the GUI (or any third‑party client) depend on this crate
to ensure a consistent contract.

When the daemon’s API stabilises, this crate may be published separately
so community frontends can re‑use the types.

## License

Same dual‑licensing as the workspace.
